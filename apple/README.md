# Apple application

Native SwiftUI/Xcode shell for safe Ethereum key/address verification and interoperability. The Apple implementation does not perform private-key cracking, seed guessing, or unauthorized credential recovery.

Targets: iOS/iPadOS and macOS. IPA export requires macOS/Xcode; signing material remains outside Git.
