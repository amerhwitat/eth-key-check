# Eth Key Check Kotlin Mobile

Safe Android Kotlin implementation for owner-authorized Ethereum address/key verification and interoperability tests. It explicitly does not implement private-key cracking, seed guessing, credential theft, or unauthorized wallet recovery.

The module is isolated under `kotlin/mobile/` and uses the portfolio's deterministic cross-language crypto envelope and 128D/P2P boundaries. Network synchronization remains opt-in and authenticated.
