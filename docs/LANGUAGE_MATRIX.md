# Chimera language matrix

`python/` is the reference research layer; `node/` is the integration layer; `java/` is the JVM layer; `cpp/` is portable native C++; `vcpp/` is MSVC/Win32; `dotnet/csharp/` targets .NET 8/9/10. All implementations share deterministic JSON/JSONL contracts and the same security boundary.
