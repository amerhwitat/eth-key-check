# Web Implementations

The web checker performs local key-format validation only. It must not transmit private keys, derive or publish secrets, or act as a wallet signer. Production wallet integrations require explicit user authorization and established wallet-provider APIs.